#include <stdio.h>

void	p(char *buff) {
	printf(buffer);
	return;
}

void n() {

	char buffer[520];

	fgets(buffer, 512, stdin);
	p(buffer);
	if (m == 0x1025544)
		system("/bin/catt /hone/user/level5/.pass");
	return ;
}


int main() {
	n();
	return 0;
}
